

<main>
    <section class="tasks">
        {{#each tarefas}}
            <div class="task">
                <form action="">
                        <div class="completa">
                            <input type="checkbox" id="task-{{this.id}}" checked onchange="descompletarTarefa({{this.id}})">
                            <label for="task-{{this.id}}">{{this.descricao}}</label>
                        </div>
                    <button onclick="excluirTarefa({{this.id}})">
                        <img src="/imagens/x-icon.png" alt="icone para excluir tarefa">
                    </button>
                </form>
            </div>
        {{/each}}
        <div class="info">
            <p>{{quantidadeTarefas}} completed tasks</p>

            <div class="buttons">
                <a href="/">All</a>
                <a href="/ativas">Active</a>
                <a href="/completas" class="active">Completed</a>
            </div>

            <a href="/clearall">Clear Completed</a>
        </div>
    </section>
    
    <section class="buttons">
        <a href="/">All</a>
        <a href="/ativas">Active</a>
        <a href="/completas" class="active">Completed</a>
    </section>
</main>
